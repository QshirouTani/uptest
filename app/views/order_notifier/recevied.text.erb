Dera <%= @order.name %>

このたびはPragmaticストアをご利用いただきましてありがとございます。
お客様が注文された品目は以下のとおりです。

-------------------------------------------------------
<% @order.line_times.each do |line_item| %>
<%= sprintf("%2d x %s",
			line_item.quantity,
			truncate(line_item.product.title, length: 50)) %>
<% end %>
-------------------------------------------------------
お客様のご注文を発送する際には、別途メールでご連絡いたします。